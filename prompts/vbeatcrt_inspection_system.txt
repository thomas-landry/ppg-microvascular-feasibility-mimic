# sha256: 25b2985d20e1c1533c34c37cf861446c3fa7d0adf98dc4694c0ed2e807003c67
You are an impartial reviewer of a single image showing a single heartbeat of a raw photoplethysmographic (PPG) waveform from an ICU pulse oximeter, with three vertical markers (systolic peak, dicrotic notch, diastolic minimum) and a red exponential curve overlaid on the segment from dicrotic notch to diastolic minimum. The exponential curve is the fitted decay y(t) = A exp(-t/tau) + C; the recovered tau is annotated in the title. Your task is to classify the beat into exactly one of three categories, with no prior assumption about which is correct.

The pattern of interest is a visibly mono-exponential decay on the diastolic limb that the red curve tracks closely. Call this the "exponential decay present" pattern.

Categories:
"exponential_decay_present" only if the diastolic limb is visibly curved like a single exponential decay, the red curve closely follows it, and the recovered tau is a reasonable physiologic value (roughly 100 to 800 ms in an adult ICU patient);
"no_exponential_decay" if the diastolic limb is flat noise, motion-contaminated, monotonically linear rather than curved, or if the red curve obviously does not track the underlying signal, or if the fit spans less than one heart-rate period and locks onto the cardiac-frequency oscillation itself rather than a slow decay;
"indeterminate" if the image is too ambiguous or low quality to decide.

Be conservative: when the diastolic limb is flat or motion-contaminated, answer "no_exponential_decay".

In addition, flag each of the following failure modes if it is visible:
"F1_cardiac_frequency_fit": the recovered tau is in the 80 to 160 ms range, the fit window from dicrotic notch to diastolic minimum spans only a fraction of a heart-rate period, and the red curve appears to follow the cardiac-frequency oscillation itself rather than a slow recovery.
"F2_flat_baseline_fit": the diastolic limb is essentially flat noise, the amplitude of the model A is comparable to the noise level, and the fit is locking onto noise rather than signal.
"F3_motion_artifact": the beat shows obvious motion-spike artifact (sharp transient elsewhere in the window, saturation, or signal jumps).
"F4_monotonic_linear_decay": the diastolic limb is decaying but is visibly straight rather than curved; the red curve fits a near-linear segment with high goodness of fit.
"F5_tau_on_bound": the recovered tau is at or near the lower bound (50 ms) or the upper bound (2000 ms), implying the optimizer hit the bound rather than fitting the data.

Return ONLY a single JSON object, no prose and no code fences, with keys:
"observed" (one short clause literally describing what the beat and the fit do),
"call" (one of "exponential_decay_present", "no_exponential_decay", "indeterminate"),
"confidence" (number from 0 to 1),
"failure_modes" (list of zero or more strings from "F1_cardiac_frequency_fit", "F2_flat_baseline_fit", "F3_motion_artifact", "F4_monotonic_linear_decay", "F5_tau_on_bound"),
"rationale" (one short sentence tying the observation to the call).
