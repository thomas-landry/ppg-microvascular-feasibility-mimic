"""Analysis and aggregation subpackage for ppgfeas.

Intended scope: the code that aggregates per-record and per-signal outputs into
the manuscript's funnel counts and summary tables, the subject-clustered
bootstrap, the sensitivity sweeps, and the synthetic known-truth checks.

Not yet included in this release: the analysis modules operate on derived tables
built from credentialed MIMIC-IV and MIMIC-IV-WDB data and are being ported from
the originating repository only after a per-file data-use-agreement and secret
review, so they are deliberately absent here. This subpackage is the home they
will land in.
"""
